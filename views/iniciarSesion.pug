doctype html
html(lang="en")
    head
        include partials/header
    
    body
        .contenedor-formulario
            h1 Inicar Sesion en UpTask

            if error 
                p.alerta.error= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo 
                    label(for="email") E-mail: 
                    input(type="email" name="email" placeholder="Tu Email" value= email)
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" placeholder="Tu Password" value= password)
                .campo.enviar 
                    input.boton(type="submit" value="Iniciar Sesion")
                .campo.acciones 
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta? Crea una gratís.
                    a(href="/reestablecer") Olvidaste tu Contraseña

